<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:VocabularySheet.Maui.AppRunner.ViewModels"
             xmlns:dtos="clr-namespace:VocabularySheet.Application.Commons.Dtos;assembly=VocabularySheet.Application"
             xmlns:controls="clr-namespace:VocabularySheet.Maui.AppRunner.Xml.Controls"
             x:Class="VocabularySheet.Maui.AppRunner.Views.WordsList"
             x:DataType="viewModels:WordsListVM"
             x:Name="wordList">

    <CollectionView ItemsSource="{Binding Words, Mode=OneWay}"
                    MinimumWidthRequest="{OnPlatform WinUI=650}"
                    SelectionMode="None">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="dtos:WordModel">
                <controls:WordListItem
                    Margin="5"
                    Word="{Binding ., Mode=OneTime}"
                    OnClick="{Binding BindingContext.OpenWordCommand, Source={x:Reference wordList}}"/>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>